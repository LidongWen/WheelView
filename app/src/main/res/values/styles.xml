<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="popUpwindow" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/alert_dialog_menu_push_up_in</item>
        <item name="android:windowExitAnimation">@null</item>
        <!--<item name="android:windowEnterAnimation">@anim/pop_exit</item>-->
        <!--<item name="android:windowExitAnimation">@anim/pop_in</item>-->
    </style>
</resources>
